# access-log-correlator-V1
Access Log Correlator - A Python security tooling project that validates access lo schemas, normalizes events and detects suspicious failed-login patterns with structured JSON output and CI-friendly ecit codes
